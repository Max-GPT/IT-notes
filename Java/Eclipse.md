## 相关日语读法
- Eclipse エクリプス

## 快捷键
`Alt` + `/` 提示代码

`Ctrl` + `+`  代码放大

`Ctrl` + `-`  代码缩小

`Ctrl` + `Shift` + `F` 格式化文档 代码

`Ctrl` + `空格` 切换 Windows 中文 / 英语 输入法

## `.metadata` 
`.metadata` 文件夹是 工作空间 workspace 下自动生成的核心配置目录，非常重要。